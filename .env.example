# Controls which home page is rendered.
# Set to "staging" to show the portfolio (HomePage).
# Any other value (or unset) shows the coming-soon page
NEXT_PUBLIC_SITE_MODE=

# Resend API key — powers the /contact form (server-only, never exposed).
RESEND_API_KEY=
